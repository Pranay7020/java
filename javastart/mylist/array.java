package mylist;

public class array {

    public static void main(String args[]) {
//------------------------------------------------------------------------------------

//-------------Reverse string
        // String abc = "niiikikta", n = "";
        // char ch[] = abc.toCharArray();

        // for(int i = ch.length-1;i>=0;i--) {
        //     n=n+ch[i];
        // }
        // System.out.println(" " +n);

//------------------------------------------------------------------------------------

//-------------Longest string without repetating characters

        // String abc = "niiikikta", n = "";
        // boolean validate[] = new boolean[256];

        // for(int i =0;i<abc.length();i++) {
        //     if(!validate[abc.charAt(i)]) {
        //         validate[abc.charAt(i)] = true;
        //        n = n+abc.charAt(i);
        //     }
           
        // }

        // System.out.println(" " +n);

//------------------------------------------------------------------------------------

        //-------------count duplicate characters and it's frequency and print
        // char arr[] = {'b','c','g','c','z','g','c'};
        // boolean validate[] = new boolean[arr.length];

        // for(int i = 0;i<arr.length;i++) {
        //     int count = 1;
        //     if(validate[i]) {
        //         continue;
        //     }
        //     for(int j = i+1 ; j<arr.length;j++) {
        //         if(arr[i]== arr[j]) {
        //             count ++;
        //             validate[j]=true;
        //         }
                
        //     }

        //       if(count > 1) {
        //             System.out.println(arr[i] + " " +count);
        //     }
        // }


//------------------------------------------------------------------------------------

        //-----------sort array
        //int arr[] = {4,6,34,5,34,100,5};
        // int max =0;
        // for(int i =0;i<arr.length;i++) {
        //     for(int j = i+1;j<arr.length;j++) {
        //         if(arr[i] >arr[j]) {
        //             max = arr[i];
        //         }
        //     }
        // }

        // System.out.println(" max : " +max);

//------------------------------------------------------------------------------------

        //-------------count duplicate number and it's frequency and print
        // int arr[] = {4,6,34,5,34,100,5,34};
        // boolean validate[] = new boolean[arr.length];

        // for(int i =0; i<arr.length;i++) {
        //     int count = 1;
        //     if(!validate[i]) {

        //          for(int j = i+1; j<arr.length;j++) {

        //         if(arr[i]==arr[j]) {
        //             count ++;
        //             validate[j] = true;
        //         }
                    
        //     }
        //     if(count > 1) {
        //             System.out.println(arr[i] + " " +count);
        //     }
               
        //     }
           
            
        // }
        
    }
    
}
